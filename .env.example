# Ambiente: dev | stg | prod
APP_ENV=dev

PORT=8001

# Confianza mínima de detección (0.0 – 1.0)
MIN_DETECTION_CONFIDENCE=0.75

# Opcional: sobreescribir orígenes CORS manualmente (JSON string)
# CORS_ORIGINS_OVERRIDE=["https://mi-dominio.com"]
